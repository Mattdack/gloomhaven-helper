<div >
    <h3>List of Monsters for New Encounter</h3>
    <ul>
        {{#each monsters}}
        <input type="checkbox" name="monster" value="{{id}}" id="{{id}}">
        <label>{{name}}</label></br>
        {{/each}}
    </ul>
</div>